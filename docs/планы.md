# Планы развития расширения (Личный список Акая)

> [!IMPORTANT]
> **ВНИМАНИЕ ДЛЯ ИИ-ПОМОЩНИКОВ:**
> Этот файл предназначен **исключительно для чтения**.
> Никогда самостоятельно не берите задачи и идеи из этого файла на реализацию, пока пользователь (Акай) прямо не попросит сделать конкретную задачу отсюда.

---

## Идеи и планы на будущее

1. **Синхронизация с онлайн-сервисом (Поэтапная реализация):**
   Добавить возможность синхронизации прогресса (настроек, отслеживаемых тайтлов, таймстаппов) через облако.
   - [x] **Этап 1: Синхронизация через WebDAV (Nextcloud / универсальный WebDAV)** — ZIP-слияние, UI (адрес, имя, код доступа), автосинхронизация при открытии панели. Без Яндекс.Диска (для него — OAuth в будущем).
   - [ ] **Этап 2: Нативная синхронизация Google Drive (для Chrome)** — интеграция с `chrome.identity`, OAuth2 авторизация в один клик.
   - [ ] **Этап 3: Портирование Google Drive под Firefox** — переход на `browser.identity.launchWebAuthFlow` для авторизации в Firefox.

2. **Уведомления о новых главах:**
   Отображение визуальных индикаторов и счетчиков новых глав на панели для произведений, имеющих активный статус отслеживания (например, в категории «Смотрю»).

3. **Мультиавторская (универсальная) поддержка и концепция шаблонов (Templates):**
   Сделать расширение универсальным, добавив поддержку не только текущего автора (LightFox), но и других авторов на Boosty:
   *   Реализовать **систему шаблонов парсинга (Parsing Templates)** под разных авторов. Так как авторы выкладывают контент по-разному, шаблоны позволят адаптировать логику извлечения номеров глав, парсинга тайтлов и распределения по подпискам под конкретный блог.
   *   Шаблоны будут динамически переключаться на основе `slug` блога (например, свой шаблон для `lightfoxmanga` и свои правила для других авторов), сохраняя единую архитектуру ядра и интерфейса сайдбара.
   *   Обеспечить гибкое развертывание библиотеки на любых блогах Boosty, где применима концепция структурированного отслеживания прогресса.

4. **Оптимизация автоскролла для длинных тайтлов (например, «Пик боевых искусств»):**
   Исправление проблемы автоматической прокрутки к главе на Boosty при переходе к ранним (старым) главам в тайтлах с очень большим числом постов (40+). Требуется разработать стабильный и надежный алгоритм автоподгрузки контента (бесконечный скролл Boosty + нажатие кнопки «Загрузить еще») без зависаний, пропусков и блокировок интерфейса.

5. **Рестайлинг и позиционирование логотипа на панели:**
   Поэкспериментировать с визуальным стилем, геометрическими размерами и точным расположением логотипа (закладки с молнией) внутри боковой панели расширения для достижения наилучшего визуального баланса.

6. **Исправление принудительной установки качества видео (Force Video Quality):**
   Функция временно заморожена и скрыта в интерфейсе расширения. Из-за изменений структуры плеера VK со стороны Boosty (двухуровневые меню, ленивый рендеринг элементов управления) функция перестала работать стабильно как в Boosty Bookmark, так и в оригинальном расширении More Boosty Remastered. Необходимо дождаться исправления бага автором оригинального расширения, после чего импортировать обновлённую рабочую логику.


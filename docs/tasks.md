# Список задач (Task List) — Boosty Bookmark

## 1. Архитектура и Ядро (Core & State)
- [x] Создать файл конфигурации `manifest.json`
- [x] Логика загрузки и кэширования постов (API + chrome.storage)
- [x] Ручной экспорт и импорт прогресса (ZIP-архив с разбивкой по каналам)
- [x] Реализовать сохранение активного тайтла (открытого детального вида) и текущей вкладки в `sessionStorage` вкладки браузера для предотвращения сброса UI при перезагрузках
- [x] Реализовать двухэтапное подтверждение сброса данных расширения с очисткой `chrome.storage.local` и редиректом на экран синхронизации
- [x] Исправить ошибку «Extension context invalidated» при обновлении расширения (cleanup, отписка от событий, восстановление history)

## 2. Интеграция с Boosty & Реактивность (Sync & Reactivity)
- [x] **Перехват fetch:** мгновенное отображение лайков с Boosty в расширении (патч `window.fetch` для POST-запросов к `/reaction`)
- [x] **Умная инкрементальная синхронизация:** замена полной 25-секундной загрузки сканированием до N известных постов подряд
- [x] **DOM-синхронизация лайка:** при отметке чекбокса в расширении программно нажимается кнопка лайка на странице Boosty для мгновенного обновления интерфейса сайта
- [x] Исправить отсутствие статуса лайков (`isLiked`) — добавлена авторизация через Bearer-токен из localStorage
- [x] Реализовать автоматическую постановку лайка на Boosty при отметке чекбокса «просмотрено» в расширении
- [x] Реализовано автоматическое присвоение статуса «Смотрю» тайтлам, в которых пользователь посмотрел больше одной главы (если статус равен `none`)
- [x] Реализован автоматический перенос в завершенные (`completed`), если тайтл полностью озвучен, все посты просмотрены и статус равен `none` или `watching`

## 3. SPA-навигация & Скролл (Navigation & Scrolling)
- [x] Добавить поддержку URL с query-параметрами (исправление бага с `?isFromFeed=true`)
- [x] Реализовать динамическое скрытие/показ панели при переходах на SPA-сайте Boosty
- [x] Реализовать настройку «Открывать тайтлы в текущей вкладке» и перехват кликов по тегам на самом сайте Boosty для бесшовного перехода через SPA
- [x] Реализовать автоматический скролл к началу ленты постов (кнопкам «Лента» / «Медиа») при переходе по ссылке тайтла из карточки расширения
- [x] Улучшенный автоскролл к главе по хэшу `#post-[id]` (интервальный поиск до 30 попыток по регистронезависимому селектору с отложенным скроллингом страницы вниз)
- [x] Реализовать опцию «Искать главы в ленте тайтла (Бета)» (`openChaptersInFeed`) с предупреждением об ограничениях и крестиком закрытия

## 4. Интерфейс и Настройки (UI/UX & Settings)
- [x] Сгенерировать иконки расширения (`generate_icons.py` через Python Pillow)
- [x] Создать файл стилей `styles.css` (темизация в стиле Boosty, темный/светлый режим, анимации, стеклянный эффект)
- [x] Сделать чекбоксы глав оранжевыми (в стиле Boosty) и заменить disabled на `pointer-events: none` для пролайканных элементов
- [x] Устранить зазор между шапкой Boosty и панелью расширения (сдвиг сайдбара на `top: 56px`)
- [x] Выполнен рестайлинг логотипов расширения (кнопки-триггера и шапки сайдбара): убран внешний оранжевый квадрат-плашка, оставлена чистая закладка с молнией
- [x] Оптимизированы геометрические размеры оранжевой подложки под молнию в логотирах сайдбара (высота уменьшена до 630px для сохранения прозрачности выреза)
- [x] Реализована автоматическая инверсия цветов логотипа в светлой теме сайдбара (`.lf-light`)
- [x] Центрирована кнопка-триггер по вертикали (`top: 50%; transform: translateY(-50%)`)
- [x] Защищена иконка кнопки-триггера от влияния темы сайта за счет явной заливки `fill="#ffffff"`
- [x] Реализовано автоматическое скрытие (сворачивание) сайдбара при нажатии на колокольчик уведомлений или аватар/меню профиля пользователя в шапке Boosty
- [x] Реализовать настройку масштаба (zoom) боковой панели с шагом (коэффициенты, автомиграция) и масштабом по умолчанию 100% (физический коэффициент 1.25)
- [x] Реализовать кастомизацию порядка вкладок категорий в панели настроек (включая кнопки «Вверх»/«Вниз» и Drag & Drop)
- [x] Изменить порядок вкладок по умолчанию (Избранное, Новое, Смотрю, Все, Архив) с обеспечением автоматической обратной миграции старых конфигураций
- [x] Сделать блоки «Синхронизация и бэкап» и «Порядок вкладок» сворачиваемыми (collapsible) для экономии места
- [x] Предотвращение сброса прокрутки (сохранение/восстановление `scrollTop` контейнера `#lf-body-content`) при любых перерисовках сайдбара
- [x] Реализован отдельный экран «О расширении и авторе» (описание, имя автора Akai, лицензия MIT, DonationAlerts/ЮMoney в одну строку, сброс скролла наверх)
- [x] Реализовать сворачивание (закрытие) панели по кнопке-стрелочке в шапке
- [x] **БАГ (Исправлено):** Устранено появление подчеркивания у ссылок названий тайтлов и глав при наведении (переопределение стилей Boosty через border-bottom и box-shadow)
- [x] Реализована логика кнопки настроек как переключателя (toggle): при повторном клике возвращает пользователя на предыдущую активную вкладку
- [x] Реализована панель разработчика (DevTools) для эмуляции даты отсечки, скрытия блока автора и автоматический скрипт сборки релиза (build.js)


## 5. Группировка и Библиотека (Categories & Sorting)
- [x] Логика разбора постов и группировки по тайтлам
- [x] Логика автоматического распределения по тирам подписок
- [x] Добавлено отображение категории (уровня подписки) мелким шрифтом под заголовком тайтла в детальном виде
- [x] Добавлено разделение на категории «Полностью озвучено» и «Завершен том» на вкладке «Все» (с автопарсингом примечаний и сканированием последнего поста)
- [x] Во вкладке «Смотрю» тайтлы с завершенным томом и 100% прогрессом опускаются в свернутый блок «Завершен том»
- [x] Добавлен крестик для быстрой очистки поисковой строки и автоматическое раскрытие категорий при активном поиске
- [x] Реализовано сопоставление ID тегов и постов с названиями из описания профиля («Об авторе») и миграция прогресса на красивые имена
- [x] Объединены вкладки «Завершено» и «Брошено» в единую кнопку-дропдаун «Архив»
- [x] Реализована интерактивная сортировка тайтлов в списке библиотеки (8 режимов сортировки)
- [x] Вынесена категория «Объявления» в отдельную группу в самом конце вкладки «Все» (с выводом постов напрямую и прогрессом прочитанного)
- [x] Добавление подкатегории «Все» (общий плоский список) и переименование категории бесплатных постов в «Бесплатные»

## 6. Поддержка медиаплееров
- [x] Реализовать автозапоминание времени (поддержка аудио/видеоплеера) с настройкой-чекбоксом в меню
- [x] Отображать прогресс воспроизведения (просмотренное время) мелким шрифтом под названиями глав в детальном виде тайтла и группе объявлений
- [x] Скрывать прогресс воспроизведения для просмотренных (отмеченных чекбоксом) глав
- [x] Автоматически сбрасывать и удалять сохраненный прогресс воспроизведения при доигрывании видео/аудио до самого конца (событие `ended`) с очисткой UI сайдбара
- [ ] Исправить принудительную установку качества видео (Force Video Quality) в плеере VK на Boosty (временно заморожено и скрыто в интерфейсе из-за изменений на стороне Boosty/VK)

## 7. Автоматизированное тестирование
- [x] Интегрировать тестовое окружение (создание `package.json`, установка Vitest и Playwright)
- [x] Написать юнит-тесты для функций парсинга, группировки и сортировки в `content.js` (`tests/unit.test.js`)
- [x] Написать E2E-тесты в Playwright (проверка рендеринга, кликов по чекбоксам, настроек и синхронизации)
- [x] Настроить мок-данные для тестов (перехват сетевых запросов к API Boosty)

## 8. Облачная синхронизация (WebDAV)
- [x] Модуль `webdav-sync.js`: клиент WebDAV, слияние ZIP-бэкапов по каналам
- [x] UI настроек: адрес сервера, имя пользователя, код доступа, мини-инструкция для Nextcloud
- [x] Синхронизация в формате ZIP (`boosty_bookmark_sync.zip`), идентичном ручному экспорту
- [x] Автосинхронизация при открытии панели (тихий режим, не чаще 1 раза в минуту)
- [x] Юнит-тесты слияния в `tests/webdav-sync.test.js`
- [x] Убраны упоминания Яндекс.Диска; OAuth для облаков — в планах (`docs/планы.md`)

## 9. Доработка WebDAV-синхронизации и исправление багов
- [x] Внедрить таймстамп `updatedAt` для тайтлов в `content.js` при любых изменениях состояния (статус, заметки, прогресс глав)
- [x] Изменить алгоритм слияния `mergeUserData` в `webdav-sync.js` на основе `updatedAt` (сравнение таймстампов вместо приоритета статуса и длины заметок)
- [x] Доработать форму настроек для корректного сброса кода доступа при его стирании и добавить маску-заглушку `••••••••`
- [x] Добавить кнопку «Очистить настройки WebDAV» в интерфейс настроек
- [x] Реализовать отложенную (debounced) авто-выгрузку изменений в облако после `saveStateToStorage()`
- [x] Добавить более понятные сообщения об ошибках авторизации (401/403) при сетевых операциях скачивания и выгрузки в `webdav-sync.js`
- [x] Добавить/обновить юнит-тесты в `tests/webdav-sync.test.js` для проверки нового слияния с `updatedAt`
- [x] Запустить unit-тесты (`npm run test:unit`) и E2E-тесты (`npm run test:e2e`) для проверки работоспособности

## 10. Локализация (Русский и Английский языки)
- [x] Создать словарь локализации `locales.js` со всеми необходимыми ключами для русского и английского языков
- [x] Интегрировать функцию `t()` для динамического перевода по ключам на основе `document.documentElement.lang`
- [x] Локализовать HTML-структуру сайдбара, форму и разделы панели настроек в `content.js`
- [x] Локализовать все всплывающие уведомления при переключении настроек, импорте/экспорте ZIP и WebDAV-синхронизации
- [x] Локализовать сообщения об ошибках и статусы синхронизации в `webdav-sync.js`
- [x] Адаптировать форматирование даты синхронизации под язык страницы
- [x] Обновить unit- и E2E-тесты для поддержки локализации и успешного прохождения

## 11. Логика вкладки «Новые» и автопереключения
- [x] Упростить названия категорий до «Новые тайтлы» и «Новые главы» с соответствующей локализацией
- [x] Сделать списки на вкладке «Новые» всегда автоматически раскрытыми
- [x] Реализовать автоматическое переключение на вкладку «Новые» при обнаружении свежих обновлений после синхронизации (инкрементальной, полной или фоновой)
- [x] Добавить инициализацию `state.lastVisit` с привязкой к дате отсечки (на 24 часа раньше) при включенной эмуляции, чтобы последние посты считались новыми
- [x] Исправить сброс `state.lastVisit` при очистке базы данных (с 0 на Date.now() / обратно в 0)
- [x] Защитить вкладку «Новые» от автоматического исчезновения новинок при обновлении страницы: `state.lastVisit` перезаписывается только тогда, когда пользователь гарантированно просмотрел новинки (переключил вкладку с «Новые» на другую, свернул сайдбар или закрыл вкладку браузера при открытом сайдбаре на вкладке «Новые»)

## 12. Исправление багов фонового процесса, DevTools, сброса данных и E2E тестов
- [x] Исправить баг `chrome.action is undefined` в `background.js` добавлением фоллбека на `chrome.browserAction` и блока `"action"` в `manifest.json`
- [x] Доработать дев-эмуляцию: предотвратить потерю и сброс `state.lastVisit` в `0` при изменении даты отсечки в будущее для корректного поступления новых постов, а также настроить автоматический запуск инкрементальной синхронизации при сохранении дев-настроек
- [x] Реализовать полный сброс и очистку визуальных дев-эффектов (таких как скрытие блока «Об авторе») и сброс `devSettings` к фабричным значениям при сбросе данных расширения
- [x] Добавить и обновить E2E тесты в Playwright: внедрить тест «Очистка новинок с двухэтапным подтверждением» и доработать тест сброса для валидации очистки DevTools настроек

## 13. Рестайлинг кнопки удаления новинки и оптимизация вкладки «Новые»
- [x] Переработать кнопку удаления новинки: заменить громоздкую оранжевую плашку на аккуратный оранжевый крестик, заменяющий текст прогресса глав при наведении на строку тайтла
- [x] Добавить плавную анимацию появления крестика (scale + opacity) и реакцию на ховер (увеличение размера и цвета на более светлый оранжевый)
- [x] Перенести общую кнопку очистки новинок «Очистить всё» в самый конец вкладки «Новые» под все списки тайтлов и глав


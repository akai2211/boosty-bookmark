tags_info.json
.tmp/
node_modules/
test-results/
.tmp_build/
*.zip

# Артефакт сборки esbuild (источник правды — src/)
/content.js
/content.js.map

# --- Локальные файлы ИИ-агентов и IDE (НЕ коммитить) ---
# Конфиги и тулинг конкретного агента/IDE не должны попадать в репозиторий и мешать
# другим агентам (Claude, Gemini/Antigravity и т.д.). Каждый агент держит своё локально.
.claude/
.mcp.json
.gemini/
.antigravity/
.vscode/
.idea/
# Личный тулинг агента: скрипты запуска браузера, отладочные профили, временные файлы.
.agent-local/
# Артефакты Playwright MCP (скриншоты, снапшоты страниц, логи консоли при live-проверке).
.playwright-mcp/

# Историческое ТЗ/брендинг — только локально, в репозиторий не попадает (удалено из всей истории).
docs/archive/

# Python cache
__pycache__/
*.pyc


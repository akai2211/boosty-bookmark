# План реализации: LightFox Boosty Bookmark

Этот проект представляет собой расширение для Google Chrome (Manifest V3), разработанное специально для страницы автора `lightfoxmanga` на Boosty. Расширение помогает зрителям отслеживать прогресс прослушивания озвучек, организовывать тайтлы по категориям и оставлять личные заметки (блокнот).

Интерфейс встраивается в правую часть экрана в виде аккуратной выдвижной панели, оформленной в едином стиле с Boosty (поддержка темной и светлой темы, лаконичный дизайн без лишней перегруженности).

---

## Ответы на вопросы и архитектурные решения

### 1. Как отслеживать просмотренное (Интеграция с Boosty)
*   **Через лайки (`isLiked`):** 
    В API Boosty каждый пост содержит флаг `isLiked: true/false` для текущего авторизованного пользователя. Так как этот флаг возвращается прямо в списке постов (по 100 штук за раз), мы можем **мгновенно и без лишних запросов** узнать, какие посты пользователь лайкнул.
    Мы добавим опцию: *«Считать лайкнутые посты прослушанными»*. Если она включена, все посты, которым пользователь поставил лайк на Boosty, автоматически помечаются как прослушанные. Это обеспечит идеальную совместимость с вашей историей на любых устройствах!
*   **Почему не комментарии:**
    Для проверки комментариев расширению пришлось бы делать отдельный сетевой запрос для каждого из 1250+ постов. Это заняло бы несколько минут и привело бы к временной блокировке вашего IP-адреса со стороны Boosty за превышение лимитов запросов (Rate Limit). Лайки работают мгновенно.
*   **Локальные чекбоксы:**
    Пользователь всегда может вручную ставить/снимать галочки прямо в интерфейсе расширения. Это состояние сохраняется локально.

### 2. Проблема подписки при разработке (Аккаунт не требуется!)
*   **Передавать аккаунт НЕ нужно.** 
    Вся метаинформация о постах (заголовки вида *«Божественный убийца я тень 1-10»*, теги, даты публикации и требуемые уровни подписки) отдается API Boosty **публично** (даже без авторизации). Мы это проверили тестами.
    Скрыто только само содержимое (плеер с аудио), но для работы трекера и блокнота плеер не нужен — нам достаточно заголовков постов и тегов. Когда расширение будет работать у вас (где вы авторизованы и имеете подписку), браузер автоматически подставит ваши авторизационные данные для лайков.

### 3. Автоматическая группировка по тирам подписки
Каждый тайтл (группа постов по уникальному тегу) будет автоматически распределяться по выпадающим спискам на основе метаданных постов:
*   **Только для девушек:** Если в названии постов содержится фраза *«ТОЛЬКО ДЛЯ ДЕВУШЕК»* или тег содержит слово *«девушек»*.
*   **Любителям пика:** Если тег равен *«пик боевых искусств»* или уровень доступа большинства постов — *«Любители пика💥»*.
*   **Любителям ютуба:** Если уровень доступа постов — *«Любитель ютуба»*.
*   **Любителям манги:** Если уровень доступа постов — *«Любитель манги😈»*.
*   **Лисямбы мои:** Если уровень доступа постов — *«Лисямбы🦊»*.
*   **Для шейхов:** Если уровень доступа постов — *«Массонский орден шейхов💎»*.
*   **Все остальные / Без тега:** Попадают в общий список.

---

## Структура интерфейса панели (Справа)

Интерфейс будет состоять из следующих разделов (сверху вниз):

1.  **Поиск и Синхронизация:** Поле для быстрого поиска тайтлов по названию и кнопка принудительного обновления БД.
2.  **Секция «Новые тайтлы»:** Тайтлы, которые появились в базе с момента вашего последнего визита.
3.  **Секция «Новые главы»:** Тайтлы из списков «Смотрю»/«Избранное», у которых вышли новые посты с момента вашего последнего захода.
4.  **Разделы статусов:**
    *   `Избранное` (разворачиваемый список)
    *   `Отслеживаю/Смотрю` (разворачиваемый список)
5.  **Разделы по уровням подписки (Категории с выпадающими списками):**
    *   `Любителям ютуба`
    *   `Любителям манги`
    *   `Только для девушек`
    *   `Любителям пика`
    *   `Для шейхов`
    *   `Лисямбы мои`
6.  **Секция «Брошено»:** Тайтлы, добавленные сюда, скрываются из всех остальных списков, чтобы не мешать.

### Цветовая индикация тайтлов (Круглые маркеры):
*   🟡 **Желтый:** Есть новые или еще непрослушанные посты/главы.
*   🟢 **Зеленый:** Все вышедшие главы полностью прослушаны (отмечены галочками или лайками).
*   ⚪ **Серый:** Прослушивание тайтла еще не начато (ни один пост не отмечен).
*   🔴 **Красный:** Тайтл находится в категории «Брошено».

---

## Детальный вид тайтла (При клике на название)

Когда пользователь кликает на тайтл, панель переключается на детальный вид:
1.  **Кнопка «Назад»** к общему списку.
2.  **Название тайтла** (со ссылкой, ведущей на отфильтрованную страницу тега на Boosty).
3.  **Панель управления статусом:** Выпадающий список для быстрого переноса в: *Избранное / Смотрю / Брошено / Сбросить статус*.
4.  **Блокнот (Заметки):** Текстовое поле индивидуально для этого тайтла. Все изменения сохраняются автоматически по мере ввода (через `debounce` в локальное хранилище).
5.  **Список постов (Главы):**
    *   Каждый пункт — это конкретный пост на Boosty (например, *«Божественный убийца я тень 11-20»*).
    *   Слева от каждого пункта — чекбокс (если отмечен, значит прослушано).
    *   Название поста кликабельно — при клике открывается страница поста в новой вкладке (мы можем автоматически ставить галочку при переходе).
    *   Сортировка: кнопка переключения порядка глав (Сначала старые / Сначала новые).

---

## Предложенные файлы

#### [MODIFY] [описание.md](file:///home/akai/projects/LightFox_Boosty_Bookmark/%D0%BE%D0%BF%D0%B8%D1%81%D0%B0%D0%BD%D0%B8%D0%B5.md) (Ваш файл с требованиями — остается нетронутым для вашей работы)

#### [NEW] [manifest.json](file:///home/akai/projects/LightFox_Boosty_Bookmark/manifest.json)
Файл манифеста расширения. Будет настроен на запуск скриптов только на страницах `https://boosty.to/lightfoxmanga*`.

#### [NEW] [content.js](file:///home/akai/projects/LightFox_Boosty_Bookmark/content.js)
Основной скрипт расширения. Будет содержать:
- Код загрузки постов через API с пагинацией и кэшированием в `chrome.storage.local`.
- Логику разбора заголовков постов и группировки по тегам.
- Логику фильтрации технических тегов и автоматического распределения по тирам.
- Рендеринг HTML/DOM интерфейса боковой панели.
- Обработчики кликов, чекбоксов, блокнота и синхронизации лайков.

#### [NEW] [styles.css](file:///home/akai/projects/LightFox_Boosty_Bookmark/styles.css)
Стили панели в дизайне Boosty. Автоматически наследует шрифты и подстраивается под темную/светлую тему сайта.

#### [NEW] [generate_icons.py](file:///home/akai/projects/LightFox_Boosty_Bookmark/generate_icons.py)
Скрипт на Python с использованием библиотеки Pillow для генерации PNG иконок (16x16, 48x48, 128x128) с изображением лисы и свитка/книги.

---

## План верификации

### Автоматические тесты
- Проверка генерации иконок: запуск `generate_icons.py` и контроль размеров файлов.

### Ручное тестирование (выполняется вами в браузере)
1. Установка расширения в Chrome в режиме разработчика.
2. Проверка первичной синхронизации при заходе на страницу LightFox.
3. Проверка автоматического разделения тайтлов по выпадающим спискам подписок.
4. Проверка смены статусов (Избранное, Смотрю, Брошено) и смены цветов индикатора (желтый, зеленый, серый, красный).
5. Проверка автосохранения заметок в блокноте при переключениях.
6. Проверка синхронизации через лайки (поставьте лайк посту на Boosty, обновите страницу и проверьте, появилась ли галочка в расширении).
